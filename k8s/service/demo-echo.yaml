apiVersion: v1
kind: Service
metadata:
  name: demo-echo
  namespace: ingress-compare
spec:
  type: ClusterIP
  selector:
    app: demo-echo
  ports:
    - name: http
      port: 80
      targetPort: http
