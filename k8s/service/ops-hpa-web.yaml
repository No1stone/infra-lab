apiVersion: v1
kind: Service
metadata:
  name: ops-hpa-web
  namespace: ops-hpa
spec:
  type: ClusterIP
  selector:
    app: ops-hpa-web
  ports:
    - name: http
      port: 80
      targetPort: http
